create table usuario(
    nombre varchar(20),
    usuario varchar(20),
    credencial varchar(20),
    sexo varchar(1),
    tarjeta varchar(16),
    region varchar(20),
    provincia varchar(20),
    distrito varchar(20),
    correo varchar(20),
    telefono varchar(9)
);
select * from usuario;